module.exports=function installFleetEnvironment(app,{db,requireAuth,requireRole,audit}){
  db.exec(`
    CREATE TABLE IF NOT EXISTS environmental_logs(
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      log_date TEXT NOT NULL UNIQUE,
      leachate_m3 REAL DEFAULT 0,
      tanker_trips REAL DEFAULT 0,
      cover_trips REAL DEFAULT 0,
      cover_quantity REAL DEFAULT 0,
      cover_unit TEXT DEFAULT 'نقلة',
      notes TEXT DEFAULT '',
      created_by INTEGER,
      created_at TEXT DEFAULT CURRENT_TIMESTAMP,
      updated_at TEXT DEFAULT CURRENT_TIMESTAMP
    );
    CREATE INDEX IF NOT EXISTS idx_environmental_date ON environmental_logs(log_date);
    CREATE TABLE IF NOT EXISTS feature_permissions(
      user_id INTEGER NOT NULL,
      feature TEXT NOT NULL,
      can_view INTEGER NOT NULL DEFAULT 1,
      can_edit INTEGER NOT NULL DEFAULT 0,
      updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
      PRIMARY KEY(user_id,feature),
      FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE
    );
  `);
  const clean=(v,n=300)=>String(v??'').trim().slice(0,n);
  const dateOk=v=>!v||/^\d{4}-\d{2}-\d{2}$/.test(String(v));
  const daysUntil=v=>{if(!v||!dateOk(v))return null;const t=new Date(`${v}T00:00:00Z`).getTime();return Number.isFinite(t)?Math.ceil((t-Date.now())/86400000):null};
  const features=['fleet','incidents','environment','global_search','backups'];
  function permission(user,feature){if(!user)return{can_view:0,can_edit:0};if(user.role==='admin')return{can_view:1,can_edit:1};const row=db.prepare('SELECT can_view,can_edit FROM feature_permissions WHERE user_id=? AND feature=?').get(user.id,feature);if(row)return{can_view:Number(row.can_view),can_edit:Number(row.can_edit)};return user.role==='editor'?{can_view:1,can_edit:1}:{can_view:1,can_edit:0};}
  function featureGuard(feature,edit=false){return(req,res,next)=>requireAuth(req,res,()=>{const p=permission(req.user,feature);if(!p.can_view||(edit&&!p.can_edit))return res.status(403).json({ok:false,message:'لا توجد صلاحية كافية لهذا القسم'});next();});}

  app.get('/api/feature-permissions/me',requireAuth,(req,res)=>{const permissions={};features.forEach(f=>permissions[f]=permission(req.user,f));res.json({ok:true,role:req.user.role,permissions})});
  app.get('/api/feature-permissions',requireRole('admin'),(req,res)=>{const users=db.prepare("SELECT id,username,display_name,role,is_active FROM users WHERE username<>'__guest_link_viewer' ORDER BY display_name").all();const rows=db.prepare('SELECT user_id,feature,can_view,can_edit FROM feature_permissions').all();res.json({ok:true,users,rows,features})});
  app.put('/api/feature-permissions/:id',requireRole('admin'),(req,res)=>{const id=Number(req.params.id),u=db.prepare('SELECT id,username,role FROM users WHERE id=?').get(id);if(!u)return res.status(404).json({ok:false,message:'المستخدم غير موجود'});if(u.role==='admin')return res.status(400).json({ok:false,message:'صلاحيات المدير كاملة'});const list=Array.isArray(req.body?.permissions)?req.body.permissions:[];db.transaction(()=>{for(const item of list){if(!features.includes(item.feature))continue;const view=item.can_view?1:0,edit=view&&item.can_edit?1:0;db.prepare(`INSERT INTO feature_permissions(user_id,feature,can_view,can_edit,updated_at) VALUES(?,?,?,?,CURRENT_TIMESTAMP) ON CONFLICT(user_id,feature) DO UPDATE SET can_view=excluded.can_view,can_edit=excluded.can_edit,updated_at=CURRENT_TIMESTAMP`).run(id,item.feature,view,edit)}})();audit(req.user,'UPDATE_FEATURE_PERMISSIONS','user',id,u.username);res.json({ok:true,message:'تم حفظ صلاحيات الأقسام'})});

  const vehicleSql=`SELECT v.*,d.name_ar AS driver_name,d.expiry_date AS driver_license_expiry,d.license_class AS driver_license_class,d.license_number AS driver_license_no,d.image_name AS driver_image FROM movement_vehicles v LEFT JOIN driver_licenses d ON d.id=v.driver_id`;
  const mapVehicle=x=>({...x,plate_no:x.plate_number,vehicle_name:x.vehicle_type,driver_license_days:daysUntil(x.driver_license_expiry),vehicle_license_days:daysUntil(x.vehicle_license_expiry),insurance_days:daysUntil(x.insurance_expiry),has_driver_image:Boolean(x.driver_image)});
  app.get('/api/ops/fleet',featureGuard('fleet'),(req,res)=>{const q=clean(req.query.q,100);let sql=vehicleSql,p=[];if(q){const like=`%${q}%`;sql+=` WHERE v.plate_number LIKE ? OR v.vehicle_type LIKE ? OR v.model LIKE ? OR d.name_ar LIKE ? OR d.license_number LIKE ?`;p=[like,like,like,like,like]}sql+=' ORDER BY v.plate_number';res.json({ok:true,vehicles:db.prepare(sql).all(...p).map(mapVehicle),permission:permission(req.user,'fleet')})});
  app.post('/api/ops/fleet',featureGuard('fleet',true),(req,res)=>{try{const b=req.body||{},plate=clean(b.plate_number??b.plate_no,30),type=clean(b.vehicle_type??b.vehicle_name,80),model=clean(b.model,80),driverId=b.driver_id?Number(b.driver_id):null,status=clean(b.status,30)||'تعمل',notes=clean(b.notes,500),vehicleExp=clean(b.vehicle_license_expiry,10),insurance=clean(b.insurance_expiry,10);if(!plate||!type)return res.status(400).json({ok:false,message:'رقم اللوحة ونوع المركبة مطلوبان'});if(driverId&&!db.prepare('SELECT id FROM driver_licenses WHERE id=?').get(driverId))return res.status(400).json({ok:false,message:'السائق المحدد غير موجود ضمن موظفي المجلس'});if(db.prepare('SELECT id FROM movement_vehicles WHERE plate_number=?').get(plate))return res.status(409).json({ok:false,message:'رقم اللوحة مسجل مسبقًا'});const r=db.prepare(`INSERT INTO movement_vehicles(plate_number,vehicle_type,model,driver_id,vehicle_license_expiry,insurance_expiry,status,notes) VALUES(?,?,?,?,?,?,?,?)`).run(plate,type,model,driverId,vehicleExp,insurance,status,notes);audit(req.user,'CREATE_MOVEMENT_VEHICLE','movement_vehicle',r.lastInsertRowid,plate);res.json({ok:true,id:r.lastInsertRowid})}catch(e){res.status(500).json({ok:false,message:'تعذر حفظ المركبة'})}});
  app.put('/api/ops/fleet/:id',featureGuard('fleet',true),(req,res)=>{try{const id=Number(req.params.id),old=db.prepare('SELECT * FROM movement_vehicles WHERE id=?').get(id);if(!old)return res.status(404).json({ok:false,message:'المركبة غير موجودة'});const b=req.body||{},plate=clean(b.plate_number??b.plate_no??old.plate_number,30),type=clean(b.vehicle_type??b.vehicle_name??old.vehicle_type,80),model=clean(b.model??old.model,80),driverId=(b.driver_id===null||b.driver_id===''||b.driver_id===undefined)?old.driver_id:Number(b.driver_id),status=clean(b.status??old.status,30)||'تعمل',notes=clean(b.notes??old.notes,500),vehicleExp=clean(b.vehicle_license_expiry??old.vehicle_license_expiry,10),insurance=clean(b.insurance_expiry??old.insurance_expiry,10);if(driverId&&!db.prepare('SELECT id FROM driver_licenses WHERE id=?').get(driverId))return res.status(400).json({ok:false,message:'السائق المحدد غير موجود'});const dup=db.prepare('SELECT id FROM movement_vehicles WHERE plate_number=? AND id<>?').get(plate,id);if(dup)return res.status(409).json({ok:false,message:'رقم اللوحة مسجل لمركبة أخرى'});db.prepare(`UPDATE movement_vehicles SET plate_number=?,vehicle_type=?,model=?,driver_id=?,vehicle_license_expiry=?,insurance_expiry=?,status=?,notes=?,updated_at=CURRENT_TIMESTAMP WHERE id=?`).run(plate,type,model,driverId,vehicleExp,insurance,status,notes,id);audit(req.user,'UPDATE_MOVEMENT_VEHICLE','movement_vehicle',id,plate);res.json({ok:true})}catch(e){res.status(500).json({ok:false,message:'تعذر تحديث المركبة'})}});
  app.delete('/api/ops/fleet/:id',requireRole('admin'),(req,res)=>{const id=Number(req.params.id),old=db.prepare('SELECT plate_number FROM movement_vehicles WHERE id=?').get(id);if(!old)return res.status(404).json({ok:false,message:'المركبة غير موجودة'});db.prepare('DELETE FROM movement_vehicles WHERE id=?').run(id);audit(req.user,'DELETE_MOVEMENT_VEHICLE','movement_vehicle',id,old.plate_number);res.json({ok:true})});

  app.get('/api/ops/environment',featureGuard('environment'),(req,res)=>{const from=clean(req.query.from,10),to=clean(req.query.to,10);let sql='SELECT * FROM environmental_logs WHERE 1=1',p=[];if(from&&dateOk(from)){sql+=' AND log_date>=?';p.push(from)}if(to&&dateOk(to)){sql+=' AND log_date<=?';p.push(to)}sql+=' ORDER BY log_date DESC';const rows=db.prepare(sql).all(...p);const totals=rows.reduce((a,r)=>({days:a.days+1,leachate_m3:a.leachate_m3+Number(r.leachate_m3||0),tanker_trips:a.tanker_trips+Number(r.tanker_trips||0),cover_trips:a.cover_trips+Number(r.cover_trips||0),cover_quantity:a.cover_quantity+Number(r.cover_quantity||0)}),{days:0,leachate_m3:0,tanker_trips:0,cover_trips:0,cover_quantity:0});res.json({ok:true,rows,totals,permission:permission(req.user,'environment')})});
  app.post('/api/ops/environment',featureGuard('environment',true),(req,res)=>{const b=req.body||{};if(!b.log_date||!dateOk(b.log_date))return res.status(400).json({ok:false,message:'التاريخ مطلوب'});db.prepare(`INSERT INTO environmental_logs(log_date,leachate_m3,tanker_trips,cover_trips,cover_quantity,cover_unit,notes,created_by,updated_at) VALUES(?,?,?,?,?,?,?,?,CURRENT_TIMESTAMP) ON CONFLICT(log_date) DO UPDATE SET leachate_m3=excluded.leachate_m3,tanker_trips=excluded.tanker_trips,cover_trips=excluded.cover_trips,cover_quantity=excluded.cover_quantity,cover_unit=excluded.cover_unit,notes=excluded.notes,created_by=excluded.created_by,updated_at=CURRENT_TIMESTAMP`).run(clean(b.log_date,10),Number(b.leachate_m3||0),Number(b.tanker_trips||0),Number(b.cover_trips||0),Number(b.cover_quantity||0),clean(b.cover_unit,30)||'نقلة',clean(b.notes,1500),req.user.id);audit(req.user,'UPSERT_ENVIRONMENT_LOG','environment',b.log_date);res.json({ok:true})});
  app.delete('/api/ops/environment/:date',requireRole('admin'),(req,res)=>{const d=clean(req.params.date,10),r=db.prepare('DELETE FROM environmental_logs WHERE log_date=?').run(d);if(!r.changes)return res.status(404).json({ok:false,message:'السجل غير موجود'});audit(req.user,'DELETE_ENVIRONMENT_LOG','environment',d);res.json({ok:true})});
};