{
  "name": "minya-landfill-system",
  "version": "3.4.0",
  "description": "Minya Landfill daily, monthly, annual and management reporting system",
  "main": "server.js",
  "scripts": {
    "prestart": "npm run build:app",
    "start": "node scripts/start-server.js",
    "build:app": "node scripts/build-app-bundle.js",
    "test": "npm run build:app && node scripts/build-app-bundle.js --check && node scripts/check-js.js && node scripts/ui-regression-test.js && node scripts/v34-regression-test.js && node scripts/smoke-test.js && node scripts/runtime-smoke-v33.js"
  },
  "keywords": [
    "minya-landfill",
    "reporting",
    "solid-waste"
  ],
  "author": "Landfill Section",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "adm-zip": "^0.6.0",
    "better-sqlite3": "^13.0.3",
    "express": "^5.2.1"
  }
}
