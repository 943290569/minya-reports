/* V31 compatibility shim: legacy V17 overlay disabled. Pivot V30/V31 is the single source of truth for landfill, stations and Aziz. */
(function(){
  // Intentionally no DOM rewrites. The old V17 overlay could overwrite correctly parsed landfill values with zeros.
})();
