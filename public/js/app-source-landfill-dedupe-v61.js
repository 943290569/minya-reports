/* Legacy landfill dedupe shim.
 * This module is intentionally disabled.
 * Landfill Pivot parsing and duplicate prevention are handled by the canonical
 * V33/V32 source adapter and the source import guard. Keeping this legacy
 * renderer active would re-write preview totals after the canonical parser.
 */
(function(){})();
