/* V25: capture original Pivot files on input change, then re-apply the real last day values. */
(function(){
  const $=id=>document.getElementById(id);
  const clean=v=>String(v??'').replace(/\s+/g,' ').trim();
  const norm=v=>clean(v).replace(/[أإآ]/g,'ا').replace(/ة/g,'ه').replace(/ى/g,'ي').replace(/[ًٌٍَُِّْـ]/g,'').toLowerCase();
  const num=v=>{const n=Number(String(v??'').replace(/,/g,''));return Number.isFinite(n)?n:0;};
  const fmt=v=>Number(v||0).toLocaleString('en-US',{maximumFractionDigits:2});
  const saved={landfill:null,stations:null,aziz:null};
  const timers=[];
  const text=x=>`${fmt(x.tons)} طن · ${fmt(x.trucks)} شاحنة`;
  function iso(v){if(v instanceof Date&&!Number.isNaN(v.getTime()))return `${v.getFullYear()}-${String(v.getMonth()+1).padStart(2,'0')}-${String(v.getDate()).padStart(2,'0')}`;if(typeof v==='number'&&window.XLSX?.SSF){const d=XLSX.SSF.parse_date_code(v);if(d)return `${d.y}-${String(d.m).padStart(2,'0')}-${String(d.d).padStart(2,'0')}`;}const s=clean(v);let m=s.match(/^(\d{4})[-\/.](\d{1,2})[-\/.](\d{1,2})/);if(m)return `${m[1]}-${String(+m[2]).padStart(2,'0')}-${String(+m[3]).padStart(2,'0')}`;m=s.match(/^(\d{1,2})[-\/.](\d{1,2})[-\/.](\d{4})/);if(m)return `${m[3]}-${String(+m[2]).padStart(2,'0')}-${String(+m[1]).padStart(2,'0')}`;return'';}
  function stationKey(v){const n=norm(v);if(n.includes('يطا'))return'yata';if(n.includes('ترقوميا'))return'tarqumia';if(n.includes('الخليل'))return'hebron';return'';}
  function bucket(v){const n=norm(v);if(n.includes('عصاره'))return'leachate';if(n.includes('هيئات محليه'))return'local';if(n.includes('اسرائيلي')||n.includes('مستوطن'))return'settlements';if(n.includes('افراد')||n.includes('نقديه'))return'individuals';if(n.includes('شركات')||n.includes('مصانع'))return'companies';return'other';}
  function emptyLand(){return{local:{tons:0,trucks:0},settlements:{tons:0,trucks:0},individuals:{tons:0,trucks:0},companies:{tons:0,trucks:0},other:{tons:0,trucks:0},leachate:{tons:0,trucks:0}};}
  function emptyStations(){return{yata:{tons:0,trucks:0},tarqumia:{tons:0,trucks:0},hebron:{tons:0,trucks:0}};}
  async function pivot(file){if(!file)return null;const wb=XLSX.read(await file.arrayBuffer(),{type:'array',cellDates:true});let best=null;for(const name of wb.SheetNames){const rows=XLSX.utils.sheet_to_json(wb.Sheets[name],{header:1,raw:true,defval:'',blankrows:false});for(let h=0;h<Math.min(25,rows.length);h++){const a=norm(rows[h]?.[0]),b=norm(rows[h]?.[1]),c=norm(rows[h]?.[2]);if((a.includes('row labels')||a.includes('تسميات'))&&(b.includes('كمي')||b.includes('مجموع'))&&(c.includes('عدد')||c.includes('وحد')||c.includes('مركبات')||c.includes('شاحنات'))){if(!best||rows.length>best.rows.length)best={rows,h};break;}}}return best;}
  async function parseLand(file){const p=await pivot(file),map=new Map();if(!p)return map;let d='';for(let r=p.h+1;r<p.rows.length;r++){const row=p.rows[r]||[],date=iso(row[0]);if(date){d=date;if(!map.has(d))map.set(d,emptyLand());continue;}if(!d)continue;const label=clean(row[0]);if(!label||norm(label).includes('الاجمالي'))continue;const x=map.get(d)[bucket(label)];x.tons+=num(row[1]);x.trucks+=num(row[2]);}return map;}
  async function parseStations(file){const p=await pivot(file),map=new Map();if(!p)return map;let d='';for(let r=p.h+1;r<p.rows.length;r++){const row=p.rows[r]||[],date=iso(row[0]);if(date){d=date;if(!map.has(d))map.set(d,emptyStations());continue;}if(!d)continue;const k=stationKey(row[0]);if(!k)continue;const x=map.get(d)[k];x.tons+=num(row[1]);x.trucks+=num(row[2]);}return map;}
  async function parseAziz(file){const p=await pivot(file),map=new Map();if(!p)return map;for(let r=p.h+1;r<p.rows.length;r++){const row=p.rows[r]||[],d=iso(row[0]);if(d)map.set(d,{tons:num(row[1]),trucks:Math.round(num(row[2]))});}return map;}
  function totalLand(x){return['local','settlements','individuals','companies','other'].reduce((a,k)=>({tons:a.tons+x[k].tons,trucks:a.trucks+x[k].trucks}),{tons:0,trucks:0});}
  async function apply(){const [lmap,smap,amap]=await Promise.all([parseLand(saved.landfill),parseStations(saved.stations),parseAziz(saved.aziz)]);const table=[...($('sourceFilesPreview')?.querySelectorAll('.source-import-table')||[])].find(t=>t.querySelector('tbody tr td:nth-child(16)'));if(!table)return;const rows=[...table.querySelectorAll('tbody tr')];if(!rows.length)return;const month=Number(clean(rows[0].querySelector('td')?.textContent).slice(5,7));const candidates=new Set([...lmap.keys(),...smap.keys(),...amap.keys()].filter(d=>Number(d.slice(5,7))===month));const last=[...candidates].sort().pop();if(!last)return;const tr=rows.find(r=>clean(r.querySelector('td')?.textContent)===last);if(!tr)return;const c=tr.querySelectorAll('td'),l=lmap.get(last),s=smap.get(last),a=amap.get(last)||{tons:0,trucks:0};if(l){[[6,l.local],[7,l.settlements],[8,l.individuals],[9,l.companies],[10,totalLand(l)]].forEach(([i,v])=>{if(c[i])c[i].textContent=text(v);});}if(s||amap.has(last)){const ss=s||emptyStations(),yata={tons:ss.yata.tons+a.tons,trucks:ss.yata.trucks+a.trucks};[[11,yata],[12,a],[13,ss.tarqumia],[14,ss.hebron]].forEach(([i,v])=>{if(c[i])c[i].textContent=text(v);});}}
  }
  function schedule(){timers.splice(0).forEach(clearTimeout);[500,1200,2500,4500,7000,10500,13000].forEach(ms=>timers.push(setTimeout(()=>apply().catch(console.error),ms)));}
  function keep(id,key){const el=$(id);if(!el)return;el.addEventListener('change',()=>{const f=el.files?.[0];if(f&&!f.name.includes('normalized'))saved[key]=f;});}
  function init(){keep('sourceFile_landfill','landfill');keep('sourceFile_stations','stations');keep('sourceFile_aziz','aziz');$('analyzeSourceFilesBtn')?.addEventListener('click',schedule);}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();