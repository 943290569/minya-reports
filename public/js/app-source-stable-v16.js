/* V31: legacy V17 overlay disabled.
   Reason: it re-parsed Pivot landfill files as flat tables and overwrote correctly parsed landfill daily values with zeros.
   Pivot V30/V31 is now the single source of truth for landfill, stations and Aziz.
*/
(function(){})();
