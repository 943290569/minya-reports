name: Patch Reviews Queue
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/patch-reviews.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 .github/patch-reviews.py
      - run: node --check server.js
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add server.js
          git commit -m "Add review queue API and route" || exit 0
          git push
