name: Patch Upload Cleanup
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/patch-upload-cleanup.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 .github/patch-upload-cleanup.py
      - run: npm install
      - run: npm test
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add server.js
          git commit -m "Clean orphaned attachment files" || exit 0
          git push
